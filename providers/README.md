# Providers

The provider registry is the source-of-truth for issuer/platform metadata.

A provider being listed here does **not** mean every item it hosts is free or certificate-bearing. Each credential record must carry its own evidence and status.

The project is intentionally source-first: official issuer pages are preferred over aggregators.
